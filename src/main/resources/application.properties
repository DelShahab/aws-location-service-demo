# Main application configuration

# Set the active profile to 'dev' by default
# To use a different profile, pass it as a JVM argument: -Dspring.profiles.active=prod
spring.profiles.active=dev

# Common settings for all environments
spring.application.name=aws-location-service-demo

# The specific configuration values are defined in the profile-specific properties files:
# - application-dev.properties: Development environment settings
# - application-prod.properties: Production environment settings
